{% extends 'layout.html.twig' %}

{% block body %}

    <header class="business-header">
        <div class="container">
        </div>
    </header>

    <div class="container">
        <section class="concept">
            <div class="col-lg-12 titleconcept" align="center">
                <h2>Un concept</h2>
            </div>
            <div class="row">
                <div class="col-md-6" align="center">
                    <img class="img-fluid rounded mb-3 mb-md-0 imgconc" src="../public/assets/image/tea-food-green-ingredient-162853.jpeg" alt="">
                </div>
                <div class="col-md-6">
                    <p class="lead">Grande maison de thé depuis 2017, VolupT vous propose des produits raffinés et rigoureusement sélectionnés, beaucoup de passion et une touche de rêve.</p>
                    <p class="lead">Notre e-shop vous propose une gamme de thé à déguster à tout moment de la journée.</p>
                    <p class="lead">Vous pouvez également venir découvrir toute notre gamme en boutique</p>
                    <div align="center"><a class="btn btn-concept-page" href="index.php?section=concept">Voir plus</a></div>
                </div>
            </div>
        </section>

        <div class="container-fluid">
            <div id="slide1">
            </div>
        </div>
        {% for category in categories %}
            {% if category.category == 'Top des ventes' %}
            <section class="tea1">
                <div class="col-lg-12 titletop" align="center">
                    <h2>{{ category.category }}</h2>
                </div>
                {% for product in products %}
                    {% if category.category == product.category %}
                        <div class="col-lg-3 col-md-6 col-sm-12">
                            <div class="card" align="center">
                                <img class="card-img-top" alt="" src="{{ product.url }}">
                                <div class="card-body">
                                    <h5 class="card-title">{{ product.product }}</h5>
                                    <p class="card-text">{{ product.descpro}}</p>
                                </div>
                                <div class="card-footer" align="center">
                                    {#<a href="" class="btn btn-panier">Ajouter</a>#}
                                    <a href="index.php?section=product&id={{ product.idproducts }}" class="btn btn-produitplus">Voir plus</a>

                                </div>
                            </div>
                        </div>
                    {% endif %}
                {% endfor %}
            </section>
            {% endif %}
        {% endfor %}


    </div>

{% endblock %}